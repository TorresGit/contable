{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tiposvoucher</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tiposvoucher.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tiposvoucher.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tiposvoucher_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tiposvoucher_edit', { 'id': tiposvoucher.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
